# Стендап | 20.05.2024

**Участники:** Сергей, Михаил, Александр, Владислав

## Что сделано

- Сергей: смотрел видео на Youtube по canvas завтра приступает к реализации Line. После приступит к слоям.
- Александр: доделал навигацию (роуты), а именно «защиту» при переходе по несуществующему id. Завтра приступает к
  изучению canvas и к инструменту.
- Владислав: завал на работе. Завтра начнет разбираться с инструментом "ластик". Если не «пойдет» (но думает, что скорее
  всего «пойдет»), есть еще задачка по истории.
- Михаил: сделал визуализацию настройки инструментов. Завтра начнет изучать canvas

## План на сегодня

- Иван: Написать тесты к API.
- Мария: Приступить к верстке личного кабинета.

## Проблемы/Вопросы

– Нужен ли карандаш? Если у кисти можно менять толщину.

- Ответ: нет

– Нужно отредактировать время. Пока отображается не в том формате. Т.к. была строчка-заглушка, а сейчас
«2025-11-22T06:50:07.793Z»

- Ответ: надо вместо строки изменить хранение на число, пример: `new Date().getTime()`

– Как сделать превью (при закрытии проекта, автоматически экспортируется в png (т.к. итак нужно сделать функционал
сохранения в png, или всё таки base64 и потом отображается как превью))

- Ответ: предлагаю хранить base64, все равно надо будет его хранить чтобы подгружать текущее состояние. И это уже
  рендерить как preview

– Доработать отображение выбранного инструмента. Т.к. сейчас иконка выделяется только если на неё нажать. Но если потом
кликнуть на «холст» выделение инструмента пропадает и непонятно, какой сейчас инструмент выбран.

– При создании проекта правильнее показывается Loader на всю страницу? Может стоит добавить его на кнопку модального
окна и не закрывать его пока не перекинет в созданный проект?

- Ответ: лучше перекинуть загрузку на кнопку

– При переключении темы не видно белый холст (canvas). Либо сделать рамку, либо фон canvas серым.

- Ответ: лучше будет сделать рамку, так проще. Если мы сделаем серый фон, в таком случае нам надо изменять также и
  палитру, потому что по-умолчанию используется черный цвет, по крайней мере пока

– Кнопку («Все проекты»), т.е, по сути, кнопка «Назад / На главную». Как-то выделить. Сделать первой, добавить значок
(«Домик» например).

- Ответ: 👍

– Canvas, если задать размеры больше, чем размер окна, то он закрывает инструменты. Нужно сделать, чтобы он не выходил
за границы редактора. Панель инструментов вперед, канвас назад и сделать ему overflow: hidden;

- Ответ: нужно сделать все меню фиксированными и поверх редактора, а сам редактор сделать scrollable

– Прозрачность слоёв. Не совсем понятно. Имеется в виду именно прозрачность белого фона? Или прозрачность каждой фигуры?

- Ответ: прозрачность каждой фигуры

– Добавить переименование слоёв (значок) и удаление.

- Ответ: удаление уже есть. Надо переименование только

– Когда будет реализован выбор цвета. Добавить в интерфейс небольшой квадратик, заданного цвета. Чтобы было видно, каким
цветом сейчас идет рисование.

– Pull requests своих инструментов делать в ветку Editor.

– Перемещение нарисованного объекта скорее всего делать не нужно. В ТЗ это не прописано, да и осуществляется в нативном
canvas сложно (наверное, мы пока не уверены). Вопрос по перемещению текста... Он должен перемещаться или нет?

- Ответ: это можно оставить в backlog

– На макете ТЗ есть история использованных ранее инструментов. А у нас нет. Нужно добавить.

– Добавить ограничение размера рабочей области (canvas) иначе начинает сильно тормозить (если выбрать, к примеру, 10000
на 10000)

- Ответ: пока можно ограничить, но в дальнейшем лучше сделать оптимизацию, к примеру не рендерить то, что выходит за
  экран

– В ТЗ есть кнопка «Сохранить проект». Тогда получается AutoSave лишний? Может добавить глочку, чтобы пользователь
решил. Либо AutoSave, либо только сам может сохранять по нажатию кнопки.

- Ответ: лучше сделать что-то вроде индикации, она скорее всего всегда будет в сохраненном состоянии, но добавить этот
  функционал и вызывать отдельно. Пользователю будет понятнее, сохранил он проект или нет

### Заметки

Пока все изучают Canvas и пытаются сделать инструменты. Следующий созвон в среду.
